---
# Playbook SERVER
# Instala Docker, copia server/ (backend Express, arquitectura hexagonal)
# y lo levanta expuesto en el puerto 4000 por HTTP plano (sin TLS).
# El HTTP sin proteccion es intencional: es lo que Kali va a interceptar.

- hosts: all
  become: true

  vars:
    app_src: /vagrant/server
    app_dir: /opt/mitm-server
    app_image: mitm-server:latest
    app_container: mitm-server
    app_port: 4000

  tasks:
    - name: Actualizar cache de apt
      apt:
        update_cache: true
        cache_valid_time: 3600

    - name: Instalar dependencias base
      apt:
        name:
          - ca-certificates
          - curl
          - gnupg
          - rsync
        state: present

    - name: Crear directorio para la llave de Docker
      file:
        path: /etc/apt/keyrings
        state: directory
        mode: "0755"

    - name: Agregar llave GPG oficial de Docker
      get_url:
        url: https://download.docker.com/linux/ubuntu/gpg
        dest: /etc/apt/keyrings/docker.asc
        mode: "0644"

    - name: Agregar repositorio de Docker
      apt_repository:
        repo: "deb [signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu {{ ansible_distribution_release }} stable"
        state: present
        filename: docker

    - name: Instalar Docker Engine y el plugin de compose
      apt:
        name:
          - docker-ce
          - docker-ce-cli
          - containerd.io
          - docker-compose-plugin
        state: present
        update_cache: true

    - name: Asegurar que Docker esta corriendo
      service:
        name: docker
        state: started
        enabled: true

    - name: Agregar el usuario vagrant al grupo docker
      user:
        name: vagrant
        groups: docker
        append: true

    - name: Copiar el codigo fuente del backend a la VM
      command: >
        rsync -a --delete
        --exclude=node_modules --exclude=dist
        {{ app_src }}/ {{ app_dir }}/
      changed_when: true

    - name: Construir la imagen Docker del backend
      command: docker build -t {{ app_image }} {{ app_dir }}
      changed_when: true

    - name: Quitar contenedor previo si existe (para poder re-provisionar)
      command: docker rm -f {{ app_container }}
      failed_when: false
      changed_when: true

    - name: Levantar el contenedor del backend
      command: >
        docker run -d
        --name {{ app_container }}
        --restart unless-stopped
        -p 0.0.0.0:{{ app_port }}:{{ app_port }}
        -e PORT={{ app_port }}
        -e NODE_ENV=production
        {{ app_image }}
      changed_when: true
